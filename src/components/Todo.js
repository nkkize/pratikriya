import React from "react";

const Todo = (props) => {
    return (
        <div>
            <p>{props.name}</p>
            <h4>Hello from TODO component!</h4>

        </div>
    );
}

export default Todo;